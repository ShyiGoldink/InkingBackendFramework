# 网络实现设计讨论记录

把网络层方案定下来之前的所有结论、被否掉的方案、还没定的问题集中记在这里，
方便换对话继续，也方便拿去别处讨论。

---

## 一、项目前提（当前代码里已经有什么）

这一节给不了解本项目的人看，先交代地基。

**模块与自检**：所有非纯数据类继承 `ShineBasicModule`，函数结尾写一条自检状态，
运行期可以用指令查看。自检状态表已加锁（多线程写会导致堆损坏，已修）。

**日志**：`ShineLog` 静态类，自身有锁，写到可执行文件同级的 `LOG/<日期>/<时分秒>.html`
（一天一个文件夹、一次运行一份文件），所以单个文件不会无限长大；
但没有任何清理策略，历史文件只会越积越多，要人工删。

**控制台**：输入线程与输出线程分离。输出走 `UIMessageLibrary` 的消息队列，
输出线程每 50 毫秒轮询一次。消息带 `delayTime`，延迟本身由任务队列实现
（排一个延迟任务，到点再把消息按"立即"重新入队），消息队列本身是一条纯粹的队列，不懂时间。

**指令系统**：`CommandCenter` / `CommandExecutor`，带引号感知的分词，命令注册在 `CommandLibrary`。

**连接池 `DatabasePool`**：静态类 + RAII 借出令牌（构造时借、析构时还，配对不可能忘）。
`IDatabase` 接口 + `MySQLDatabase` 实现。已具备：借出时探活（默认每次借出都 ping）、
坏连接在归还时直接丢弃、按空闲时长主动淘汰、连接数上下限（一般 10 / 忙时 20）、
世代计数器（防止旧配置的连接回到重建后的池里）。

**线程池 `ThreadPool`**：固定 10 条线程，`init(predicate, execute, waitHint)` 三个回调。
所有线程等同一个条件、醒来做同一件事。

**任务队列 `TaskQueueLoop`**：单例，包着上面那个线程池。支持延迟任务（内部按到期时刻排序）。
任务是 `Task<std::any>`，带依赖/后继，可以表达有向无环图。

**这两件事是分开的，这点很关键**：连接池负责"有限的连接怎么复用"，
任务队列负责"异步的活谁来干"。它们各自用自己的线程，互不依赖。
连接池的"空闲连接清理"本身就是一个跑在任务队列里的定时任务（每 30 秒一轮，跑完把自己排下一次）
——任务队列已经有一个真实使用者了。

**环境**：MySQL 8.4.11 在本机（`default` 库、`healthcheck` 表）；Linux 开发，
CMake 支持 Windows/macOS；C++20。

---

## 二、网络这块已经定下来的结论

### 1. 分片模型

N 个分片，**每个分片一条独立线程**，绑定一批连接。连接的归属在**连接建立时**决定一次，
之后这个连接的所有消息都由那个分片读。不是"每条消息到达时判断"——分片就是收消息的那个东西，
它从哪个 fd 读出来，消息就是哪条连接的。

分片的理由是**隔离**：一个坏客户端只影响它所在分片里的其他连接。

### 2. IO 模型：非阻塞 + poll

- 阻塞与否是 socket 的属性，不是 `recv` 的属性。设成非阻塞之后：
  有数据返回字节数、对端关闭返回 0、暂时没数据返回 -1 且 `errno` 为 `EAGAIN`、真错误返回其它 errno。
- 所有 IO 调用都不许自作主张等待，**把"等"集中到唯一一个地方：`poll`**。
- 不用 epoll：`poll` 三个平台都有（Windows 上是 `WSAPoll`），分片这个规模下 O(n) 扫描等于零成本。
  epoll 的价值要几千个连接一个线程才体现。
- 跨平台差异压缩到两个函数里：设非阻塞（`fcntl` / `ioctlsocket`）、等就绪（`poll` / `WSAPoll`）。
  上层从 accept 到分帧到分发一行都不用动。

### 3. 分片循环每轮做什么

```
1. 把 acceptor 交过来的新连接收进来
2. 把"待发送队列"里的字节倒进各连接的写缓冲
3. poll 自己那批 fd，同时关心可读和可写
4. 就绪的连接：可读就 recv 进读缓冲并拆包；可写就把写缓冲发出去
```

写缓冲是必须的：非阻塞 `send` 可能只发出去一半，剩下的要留着等下一次可写。

### 4. 分帧：4 字节长度前缀

TCP 是字节流，消息边界不会替你保留。一条消息可能分几次到达，也可能一次到两条，
所以每条连接必须有读缓冲，拼够完整一条才往上交。

### 5. 同一条连接内必须保序

跨连接谁先谁后无所谓。但同一条连接的请求 A、B 若被两个工作线程并行处理，
B 先算完就先发出去，那是协议错误。

选定做法：**一条连接同时只允许一条消息在飞**。分片读出一条交给任务队列之后，
把这条连接的 `POLLIN` 摘掉；等它的响应进了写缓冲再恢复读取。

一个机制同时解决三件事：顺序天然正确（不需要序号）、下游积压时自然少读（背压）、
客户端猛发也灌不爆内存。

### 6. 所有权边界（最容易写错的地方）

**socket 归分片所有，工作线程不碰 socket。**

工作线程的产出只能是字节，必须通过分片的"待发送队列"交回来：

```
工作线程：算好了 → 把字节塞进该连接所属分片的待发送队列（加锁）→ 唤醒分片
分片线程：轮询时把它倒进各连接的写缓冲 → POLLOUT 时发出去
```

不能让工作线程自己调 `send`：写缓冲是分片线程读写的数据结构，直接动就是数据竞争；
而且非阻塞 `send` 的"只发一半"需要有人记着，那是分片的活。
这样连接状态永远只有分片线程一个人碰，不需要每条连接一把锁。

### 7. 分片线程是独立线程，不属于任务队列

**池子的前提是线程可以互换**——谁醒了谁干，任务交给谁都一样。
分片线程恰好相反：它绑定了固定的一批 socket 和缓冲，不能换人干。
把分片塞进池子，等于让池线程变成专用线程，池子名存实亡。

所以：分片自己开线程，业务处理复用现有的任务队列。

### 8. 不用 `detach()`

分片线程要碰 socket 表、读写缓冲、日志、任务队列，**每一项都有生命周期**。
`detach()` 之后没人能保证它在进程退出前停下来，而退出路径上主线程正在析构那些静态对象
（`ShineLog` / `UIMessageLibrary` / `DatabasePool` / `TaskQueueLoop`），
分片线程还在往里写——use-after-free，表现为"有时候退出时崩一下"。

正确形状：把 `std::thread` 存着，提供 `stop()`，析构里 `join()`。
**`poll` 必须带超时**，否则 `stop()` 设了标志位也没人叫醒它，`join()` 永久挂住——
那比 detach 更糟。想做到"一停就停"就加一个自管道/eventfd 去叫醒 `poll`。

### 9. 回调的位置

回调挂在"**拆包完成、拿到一条完整消息之后**"，用来区分连接的状态
（握手中 / 已认证 / 正常收发 / 正在关闭），不同状态下同一条连接的处理逻辑不一样。

**但回调不能替代 `poll`。** 回调回答"事情发生之后走哪段代码"，
`poll` 回答"事情发生了没有"。回调需要一个调用它的人，没有人在等就没人会调它。

---

## 三、被否掉的方案以及原因

| 方案 | 为什么否掉 |
|---|---|
| 一个线程管 10 条连接 | 数字太保守。`poll` 管 10 条和管几百条代码一样，分片数应按隔离粒度和核数定 |
| 每条分片两条线程（一收一发） | 多余。一条 `poll` 循环同时管可读和可写即可；拆两条反而要共享连接表、加锁 |
| 把分片塞进任务队列的线程池 | 做不到：`ThreadPool` 一个实例只有一份谓词和一份执行回调，所有线程干同一件事；而且两者等待条件不同（条件变量 vs `poll`） |
| 分片线程用 `detach()` | 丢句柄、退出时和静态对象析构赛跑、测试无法确定性停机 |
| 用回调代替 `poll` | 回调需要一个调用它的人。"谁在等"这件事必须有人做：自己 `poll`、内核阻塞 `recv`、或者库内部替你 `poll` |
| 所有连接一直接受消息、无条件投递 | 客户端猛发会灌爆任务队列（队列无上限）。改用"一条连接一条在飞" |

---

## 四、还没定的事

1. **分片数**、**每个分片最多多少条连接**。
2. **acceptor 放哪**：独立线程，还是 0 号分片兼做。
3. **分片怎么被唤醒**：`poll` 用短超时（简单，最多几十毫秒延迟），还是自管道/eventfd（零延迟，多一个 fd）。
4. **消息格式**：`protobuf` 的 delimited 分帧，还是自己加 4 字节长度前缀 + protobuf 负载。
   分帧形式已定为长度前缀，剩的是负载编码。
5. **任务队列满了的策略**：除了摘 `POLLIN`，队列本身要不要设上限。
6. **连接生命周期**：空闲多久断开、心跳周期多少（心跳工具最初就是为这一步准备的）。

---

## 五、相关实测数据（本机，Linux，MySQL 8.4.11，回环）

| 项目 | 数值 |
|---|---|
| `mysql_ping`（借出探活） | 15 ~ 19 微秒 |
| `SELECT 1` | 16 ~ 35 微秒 |
| 一次完整重连 | 5.7 ~ 8.4 毫秒 |
| 自检 `setStageStatus` + 写日志（单线程） | 4.5 ~ 5.6 微秒 |
| 同上，8 线程并发 | 9.2 ~ 9.5 微秒 |
| MySQL 服务端 `max_connections` / `wait_timeout` | 151 / 28800 秒 |

用途：判断"每次借出都探活"值不值（值，比重连便宜两个数量级）、
判断框架自身开销够不够低（低，但日志文件没有轮转是隐患）。

---

## 六、建议的落地顺序

**第一步：`NetworkQueue` 类**（一个实例一个分片）
`poll` + 非阻塞 + 读缓冲 + 写缓冲 + 长度前缀分帧 + 空闲超时 + 干净停机。
先跑通两个分片互不干扰，用测试客户端压收发和拆包，挂 TSan。
这一步不接任务队列。

**第二步：接任务队列**
读到完整消息 → 丢队列（带上"属于哪条连接"的句柄）→ 工作线程产出字节 →
塞回该连接所属分片的待发送队列。加上"一条连接一条在飞"。

**第三步：协议与分发**
protobuf 消息结构 + 消息 ID 到处理函数的映射，接到现有指令分发层。

**第四步：心跳与超时**
空闲断开、半开连接探测，复用连接池那边"探活 + 空闲淘汰"的思路。

---

## 七、可以直接看的参考实现

`tests/reactor_demo.cpp` —— 已经跑通的单分片骨架：`poll` 驱动、非阻塞、
读缓冲、写缓冲、长度前缀分帧、干净关闭。约 290 行（含注释），核心循环大概 60 行。

`tests/reactor_client.py` —— 配套客户端，故意用三种发法打它：
一次发完一条、把长度前缀和负载拆成两次发、一次发两条。
后两种用来证明读缓冲和拆包是必须的。

跑起来：`tests/run_tests.sh reactor`

注意：`tests/` 已被 `.gitignore` 忽略（里面硬编码了本机数据库账号密码），
换机器要手动把整个目录拷过去。
